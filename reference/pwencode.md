# Encode a password

Encode a password

## Usage

``` r
pwencode(label = "VAR", key)
```

## Arguments

- label:

  label used to encode the password

- key:

  password key

## Value

Character string, encoded password
