# A temporary directory which (1) every machine in the cluster has access to and (2) has sufficient space

A temporary directory which (1) every machine in the cluster has access
to and (2) has sufficient space

## Usage

``` r
ribiosTempdir()
```

## Value

a character string of the directory name

## See also

[`ribiosTempfile`](https://bedapub.github.io/ribiosUtils/reference/ribiosTempfile.md)
